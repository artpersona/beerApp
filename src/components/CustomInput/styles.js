import { StyleSheet } from "react-native";
import Colors from "../../screens/CheckoutScreen/Colors";
export default StyleSheet.create({
  labelStyle: {
    color: Colors.header,
    fontSize: 12.5,
    fontWeight: "500",
  },

  inputStyle: {
    fontSize: 11,
    marginHorizontal: 10,
  },
});
