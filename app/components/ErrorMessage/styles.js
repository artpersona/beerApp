import { StyleSheet } from "react-native";

import { Colors } from "../../config";

export default StyleSheet.create({
  errorMessage: {
    alignSelf: "center",
    color: Colors.error,
    marginVertical: 8,
  },
});
