import React from "react";
import { View } from "react-native";

const Divider = () => (
  <View
    style={{
      top: 10,
      borderBottomColor: "white",
    //   borderBottomWidth: 0,
    }}
  />
);

export default Divider;
